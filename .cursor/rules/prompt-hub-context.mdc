---
description: Prompt Hub 项目总览；新对话先读 PROJECT_CONTEXT
alwaysApply: true
---

# Prompt Hub 协作约定

- 新任务先读 `docs/PROJECT_CONTEXT.md`（含**当前部署阶段**）。
- 用户是纯小白：用分步说明 + 可复制命令；Cloudflare 写清菜单路径。
- 部署/API 问题见 `docs/FIX-API-522-BEGINNER.md`、`docs/CUSTOM-DOMAIN.md`。
- **P0（社区排版 / 卡片库图片）拉满算力**：见 `docs/AI-WORK-MODE.md`，禁止最小 diff 试探，允许多文件联动与整段重写。
- 简体中文；不提交密钥；仅用户要求时再 git commit。
- **只改任务相关文件**：勿顺带改无关布局/动效（如灯箱按钮、加载扫光 `card-beam-trbl`、大图样式）；生图加载中复用 `media-shine-reveal` 同一光带时用 `card-beam-loading` 循环，勿另写一套扫光。
