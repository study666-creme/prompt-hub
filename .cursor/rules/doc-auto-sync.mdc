---
description: 每 8 轮用户消息自动更新项目文档；小白友好说明
alwaysApply: true
---

# 文档自动同步

## 触发条件

- Hook 会在每第 **8** 条用户消息时创建 `.cursor/PENDING_DOC_UPDATE`。
- 若该文件存在，**在本轮回复中优先**更新 `docs/PROJECT_CONTEXT.md` 的 **「当前部署阶段」** 小节，然后删除 `.cursor/PENDING_DOC_UPDATE`。
- 计数状态见 `.cursor/doc-sync-state.json`（勿手动改 interval，默认 8）。

## 更新内容（保持简短）

- 日期、Worker 名、API 域名、Pages 地址
- 已打通 / 未打通（如 `/health`、兑换、生图）
- 已知问题与下一步（1～5 条 bullet）

## 与用户协作

- 用户是纯小白：步骤用「第 1 步 / 第 2 步」、给可复制命令；Cloudflare 控制台操作写清菜单路径。
- 最小 diff；不提交 `.env`、`*.local.js`、`service_role` 等密钥。
- 默认简体中文回复。
