# Changelog

本文件只记录用户可感知或架构级变化；完整细节以 Git 提交历史为准。

## 2026-09-20（后台：画布模型空白修复 + 生图任务分组 + 侧栏分组）

- **「画布模型」页不再一片空白**（两个 bug 叠在一起）：
  1. 路由 key 是 `canvasModels`，但面板 id 写成了 `panel-canvas-models`，
     `showTab()` 拼出的 `panel-canvasModels` 找不到元素，于是**所有**面板都被设成
     hidden —— 整页白屏、控制台还不报错。id 统一为 `panel-canvasModels`，
     并在 `showTab()` 里加了「找不到面板就回退概览 + console.warn」的兜底。
  2. 内嵌的 canvas-api 管理页响应头带 `x-frame-options: DENY`，浏览器直接拒绝在
     iframe 里渲染。新增同源反代 `functions/canvas-admin/[[path]].js`
     （`/canvas-admin/*` → canvas-api.prompt-hubs.com），剥掉该响应头，并把页面内
     写死的 `/api/v1/admin/*` 改写回 `/canvas-admin/api/v1/admin/*`。
- **画布模型页补「管理密钥 + 连接并载入」条**：密钥只存本浏览器 sessionStorage，
  进页面自动回填并触发内嵌页的「加载」，外层同步显示加载结果；载入超时给出
  重试 / 新窗口打开兜底，不会再出现无提示的空白。
- **生图任务页收成二级标签**：原先模型映射、视频模型、按模型统计、错误日志、
  最近任务 5 个大卡片一路堆下去，页面长得没法看。现在拆成
  最近任务 / 模型目录 / 模型统计 / 错误日志 四个标签，顶部统计卡常驻，
  切标签只做显隐、不额外发请求。
- **侧栏按业务域分组**：13 项平铺 → 概览 / 运营 / 内容 / 生成 / 系统，
  卡片库与画布相关入口不再混在一起，侧栏内容超高时可滚动。

## 2026-09-17（质量档位：最高/超高/自动档恢复可用）

- **修复「选最高档生图必失败」**：提交校验的 quality 枚举停在
  low/medium/standard/high/ultra，比 API 站目录实际下发的档位旧两代——
  `gpt-image-2.5` / `gpt-image-2.5-sunburst` 支持 low/medium/high/xhigh/max，
  `grok-imagine-image-2.0` 带 auto。选「最高」提交会直接返回
  「质量参数仅支持 low、medium、standard、high 或 ultra」，生图发不出去。
  现在枚举由 `GENERATION_QUALITY_TIERS` 统一提供，并覆盖全部实际档位；
  提交校验、模型列表价格预览、`/cost` 报价三处共用同一份清单，不会再各自漂移。

## 2026-09-15（界面回归：内嵌画布恢复 + 日光对比度 + 瀑布流铺满 + 背景降频）

- **恢复站内内嵌无限画布**：2026-09-06 的架构合并 `e875103` 把内嵌页 `pageCanvas`
  整块删掉（只剩侧栏一个纯外链按钮）。侧栏「无限画布」重新变回站内页面
  （路由 `/canvas/`，切到该页才挂 iframe），页头保留「在新标签页打开」独立入口。
  新增 `scripts/verify-ui-regression-guards.mjs`，避免同类功能再被合并静默带走。
- **生图页日光模式对比度**：右侧栏目段控件（最近 / 获取）与社区筛选标签原先把文字
  和滑块写成深色面板专用的半透明白，浅色面板下整排不可见；提示行
  「获取 · 点击图片放大 · 点击卡片查看详情」也是写死的 56% 白。现在按主题取色。
- **社区瀑布流不再整列留白**：追加批次按「图片还没加载出来的占位高度」分列，图片真实
  高度（一百多到四百多像素）落地后列高越走越歪，于是出现整列空白。新增测量驱动的
  再平衡（用户停手 + 空闲 320ms + 列高极差超过两张卡才重排，1.5s 限流、每批 10 次额度，
  并把视口里那张卡钉回原位）。实测 96 张卡 5 列列高极差从 ~2100px 降到 ~450px。
- **我的主页背景不再像卡死**：社区/生图/我的主页/画布原先直接整帧 pause 掉 WebGL
  背景，留下一张静止网格。改为降频到 18fps 继续跑。

## 2026-09-07（后台重构补齐：视频模型 + 统计 + 错误日志）

## 2026-09-07b（每日积分流水修复上线）

- **每日免费积分全程入账流水**：会员每日领取（daily_grant）、签到奖励（daily_checkin）、跨日过期清零（daily_expire）、切模式作废全部写入 credit_ledger。此前每日池直接改 profiles 不留痕，用户“余额变了但明细没记录”。扣费/退款侧的每日池记账已由 9-6 基线的 consume/refund_user_credits RPC 覆盖（合并时特意避免 Worker 侧重复记账），本次补齐发放侧。
- 留痕失败不阻断积分主流程（insertLedgerRow 双层容错：error 对象 + 异常均只告警）。
- 流水 balance_after 统一用可花总额（永久+当日），与钱包顶栏一致。
- 同步合并了两个工作树的全部历史（回滚事故的彻底收口）：Canvas 视频适配器、CDN 修复、部署闸门、后台重构现在都在同一条 main 上。

- 生图任务页新增**视频模型映射**：卡藏目录中的全部视频模型（公开 ID/实际模型/实时价格/参数），补齐此前只展示生图模型的缺口；视频上下架跟随卡藏渠道可用性。
- 生图任务页新增**按模型统计**：近 7 天每个模型（图片+视频合并）的任务总量、成功数、失败次数、成功率、用户数、净积分消耗。
- 生图任务页新增**错误日志**：近 7 天失败任务（图片+视频、画布与卡片库入口合并），含错误消息、扣费与退款状态——错误溯源直接在 API 侧后台完成，不依赖各前端入口各自记日志。
- 最近任务列表区分视频/生图类型（视频带时长与进度阶段）。

## 2026-09-06（后台重构）

- 前端从 3075 行同步 XHR+eval 单 IIFE 重写为原生 ES modules（`admin/modules` + `admin/views` + `admin/main.js`），hash 路由（`#/users` 等），导航重组为 10 个分组；旧 `legacy/admin` 分片归档至 `legacy/_retired-admin/`。
- 新增**订单管理页**：`payment_orders` 表（订单迁出 `activation_codes.note`，下单/回调双写），人工补单（apply_credit_delta 幂等写流水），迁移前历史订单只读查询。
- 新增**积分流水页**：credit_ledger 按用户/原因筛选、CSV 导出、用户详情直链。
- 新增**操作审计**：admin_audit_logs + 中间件，所有后台写操作记录密钥指纹、前后值、IP。
- 新增**用户封禁/解封**（profiles 封禁列 + Auth 同步 ban）。
- 修复后台改积分不写流水的账目缺口（改走 apply_credit_delta RPC）。
- 下线桶内孤儿视图、生成服务日志代理、调用链路列与 5 个死接口。
- 数据库迁移：`supabase/migrations/20260906200000_admin_console_rework.sql`（建 payment_orders / admin_audit_logs、profiles 封禁列、credit_ledger 幂等索引）。

## 2026-08-31

- 画布以新目录公开 ID（`gpt-image-2-ext`）提交生图时不再报“所选模型不可用”：Worker 生图目录补齐该 ID 到内部 `image2-pro` 线路的别名（`image-models-catalog.ts` 的 LEGACY_MODEL_MAP），并新增回归测试覆盖“live 目录快照 → 解析到 image2-pro → 提交 New API”链路。
- 视频任务状态不再依赖客户端轮询：Worker 定时任务（每 2 分钟）自动核对仍为“生成中”的视频任务，上游已完成/失败即落库并自动退款。
- 上游状态读取增加瞬时失败重试，一次 5xx/429 不再中断观察；上游明确返回“任务不存在”时任务会如实失败并退款，而不是永远停在“生成中”。
- 上游网关（New API）对可灵等视频平台改为实时拉取状态，并兼容可灵新增的 `expired`/`cancelled`/`in_queue` 状态，避免终态任务长期悬挂。

## 2026-07-11

- 生图目录收敛为已接通的卡藏 API 全能模型2/香蕉与 Apimart MJ；全能模型2新增 `0.025 元/次`特价 1K。卡藏 API 报价已包含其加价，站内按人民币价格乘 100 直接换算积分，不再重复加价。
- 修复提交生图或任务失败时全量刷新最近作品，导致其他已加载图片短暂或持续变黑的问题。
- 运营后台新增“卡藏模型 → API 模型 → 实际渠道”只读链路；候选渠道、真实调用名、分组与启停状态通过双端共享密钥保护，不进入公开模型目录。
- 修复 New API 即时图片响应偶发被判作 `missing_task_id`，以及已退款失败任务仍长期显示“恢复中”。
- 积分充值取消折扣和赠送会员天数，所有积分包固定按 `1 元 = 100 积分`定价。
- 精简运营后台旧供应商、重复社区维护和冒烟入口；存储巡检改为按需扫描当前 R2/MemFire 主存储。
- 修复后台错误检测旧生图密钥、账号删除遗漏 R2 对象和旧模型 ID 回退问题。
- 清理过时补丁、测试页、缓存文件和重复交接文档，建立统一文档索引。
- 将主站 canonical、Open Graph、sitemap、robots 和扩展登录链路统一到 `prompt-hubs.com`。
- 新增文档断链检查与 GitHub Actions CI。
- 更新隐私政策中的 MemFire、R2 和浏览器扩展数据说明。

## 2026-07-10

- 移动端卡片库首批收敛为 12 张，滚动时按页继续加载。
- 社区首屏只渲染 12 张，进入视口后渐进加载，避免一次渲染上百张。
- 修复刷新 `/prompts/` 被错误跳回社区的问题。
- 加强移动端首屏、脚本响应类型和生产资源体积审计。

## 2026-07-09

- 接入 New API 图片提供商，并统一进入 Prompt Hub 模型、计价和扣费链路。
- 收紧图片提供商提交参数与失败处理。
- 优化运营后台卡片库巡检界面。

## 2026-07-08

- 上线 Worker/API 错误、图片 404、生图失败率、请求量和积分流水监控。
- 完成 MemFire 数据库切换后的前端、Worker 和本地开发配置。
- 修复卡片图队列拆分、空图片占位和前端大文件拆分回归。

## Earlier

2026-07-08 以前的修复记录不再维护为独立说明文件，请使用 `git log` 和 `git show <commit>` 查询。
# Changelog

## 2026-09-01

- 回访加载提速：版本化静态资源（`pack-*.js`、合并后的样式表、vendor 等）从每次全量重下（`no-store`）改为 `immutable` 一年长缓存，失效仍由 `bump-build.ps1` 刷新 `?v=` 完成；入口 HTML 与 `sw.js` 保持不缓存。新增 predeploy 守卫拦截「内容变了但版本没 bump」（否则长缓存会把旧内容锁死）。
- 安全加固：API 的 CORS 删除任意 `*.vercel.app` 与任意 `chrome-extension://` 通配放行，只保留 `CORS_ORIGINS` 精确域名、自有 pages.dev 预览域和本机开发地址。
- 稳定性加固：11 处付费/上游 fetch 全部补上超时（提交 120s、对话 90s、轮询 15s 等），卡死的上游连接不再把 queue consumer 整批挂满；付费 POST 超时沿用既有「未知结果→退款 SLA」路径，不新增重试。
- 恶性 bug 修复（第二批）：
  - **settle 轮询自旋**：`settle=1` 请求内最多同步等待 ~105s 必触发边缘 524；所有用户路径轮询循环改用 80s 预算时钟裁剪尝试次数，recover-warehouse 批处理耗尽预算后把剩余任务留给下一轮。
  - **chat 双扣费窗口**：对话接口原为「先调用上游后扣费」，客户端网络重试会重复扣费、上游成功但余额不足时平台漏收。现改为预扣最坏情况 + 失败退款 + 成功按实际用量结算差价，支持 `clientRequestId` 幂等（前端已自动携带），上游同时带 `Idempotency-Key`。
  - **96MB 请求体**：参考图 schema 理论上限可在校验前打满 Worker 128MB 内存；生成/视频请求现在用 Content-Length 在 JSON 解析前预检（32/64MB 上限，413 拒绝）。
  - **队列重复入队**：前端轮询会让同一 newapi 任务反复入队、每条重复消息最多空转重试 20 次；现轮询侧补投限 3 分钟一次。
- 滚动性能：图片视口排序与瀑布流重排消除排序/分配循环内的强制布局读取（预读 rect/卡高后纯数字比较），渲染与补刷期卡顿减少，动效观感不变。

## 2026-08-31

- 昼夜切换移出设置面板：桌面对话侧栏底部、手机底栏新增「昼夜」按钮（`#themeToggleBtn` / `#themeToggleBtnMobile`），点击即可手动切换并暂停自动；自动昼夜默认开启（早 8 点日光 / 晚 8 点夜间），可在设置 → 外观关闭。
- 修复日光模式下图片生成页侧栏「仓库 / 获取」分段、社区排序与提示文字白字贴浅底不可读的问题（`--imagegen-segment-*` 增加浅色方案）。
- 修复日光模式下卡片库分组/模型等下拉菜单深底贴深字的问题（浅色主题补齐 `--warehouse-raised-2` 与悬停/选中高亮）。
- 生图表单的原生 `<select>`（速度、附加开关、画质、风格、分辨率、比例、质量、入库文件夹、数量、裂变/灵感抽卡/优化场景、仓库筛选等）全部替换为应用绘制下拉（`imagegen-select-ui.js` `+ .ph-cselect*`），原生 select 仅作状态载体保留。
- 修复手机端卡片库新建/生图输入区内容超过固定高度时向下溢出、把下方「卡片库」导航条盖住一半的问题（组合器禁止 flex 收缩）。

## 2026-08-06

- New API 上游从已停机的临时 IP 主机迁移到 `newapi.prompt-hubs.com` 稳定域名，后续服务器迁移通过 DNS/TLS 切换，不再要求 Prompt Hub 绑定具体 IP。
- 发布验证新增稳定域名回归守卫，并要求生产模型目录具有非空版本且不处于 stale 状态后才进行付费生成。

## 2026-07-26

- 更新全能模型 2 的公开模型契约：`全能模型2 · 特价 1K` 当前按 2.2 积分展示，`全能模型2 · 4K` 使用 canonical ID `image2-4k-fast`。
- 4K 文生图不再要求参考图；请求固定 `resolution=4k`、`quality=standard`、`n=1`，保留比例和可选单图/多图参考参数。
- 高质量模型公开提供 `1K/2K/4K`，质量选项统一为 `低 / 中 / 高`；移除 `Adobe`、`极速 4K` 等旧的公开名称。

本文件只记录用户可感知或架构级变化；完整细节以 Git 提交历史为准。

## 2026-07-11

- 生图目录收敛为已接通的卡藏 API 全能模型2/香蕉与 Apimart MJ；全能模型2新增 `0.025 元/次`特价 1K。卡藏 API 报价已包含其加价，站内按人民币价格乘 100 直接换算积分，不再重复加价。
- 修复提交生图或任务失败时全量刷新最近作品，导致其他已加载图片短暂或持续变黑的问题。
- 运营后台新增“卡藏模型 → API 模型 → 实际渠道”只读链路；候选渠道、真实调用名、分组与启停状态通过双端共享密钥保护，不进入公开模型目录。
- 修复 New API 即时图片响应偶发被判作 `missing_task_id`，以及已退款失败任务仍长期显示“恢复中”。
- 积分充值取消折扣和赠送会员天数，所有积分包固定按 `1 元 = 100 积分`定价。
- 精简运营后台旧供应商、重复社区维护和冒烟入口；存储巡检改为按需扫描当前 R2/MemFire 主存储。
- 修复后台错误检测旧生图密钥、账号删除遗漏 R2 对象和旧模型 ID 回退问题。
- 清理过时补丁、测试页、缓存文件和重复交接文档，建立统一文档索引。
- 将主站 canonical、Open Graph、sitemap、robots 和扩展登录链路统一到 `prompt-hubs.com`。
- 新增文档断链检查与 GitHub Actions CI。
- 更新隐私政策中的 MemFire、R2 和浏览器扩展数据说明。

## 2026-07-10

- 移动端卡片库首批收敛为 12 张，滚动时按页继续加载。
- 社区首屏只渲染 12 张，进入视口后渐进加载，避免一次渲染上百张。
- 修复刷新 `/prompts/` 被错误跳回社区的问题。
- 加强移动端首屏、脚本响应类型和生产资源体积审计。

## 2026-07-09

- 接入 New API 图片提供商，并统一进入 Prompt Hub 模型、计价和扣费链路。
- 收紧图片提供商提交参数与失败处理。
- 优化运营后台卡片库巡检界面。

## 2026-07-08

- 上线 Worker/API 错误、图片 404、生图失败率、请求量和积分流水监控。
- 完成 MemFire 数据库切换后的前端、Worker 和本地开发配置。
- 修复卡片图队列拆分、空图片占位和前端大文件拆分回归。

## Earlier

2026-07-08 以前的修复记录不再维护为独立说明文件，请使用 `git log` 和 `git show <commit>` 查询。
