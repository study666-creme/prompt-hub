# Copy to .dev.vars (never commit real secrets).
# Current production DB is MemFire. Use the MemFire API URL and service_role key.
# /supabase auth proxy needs SUPABASE_URL; API routes and /health need service_role.

SUPABASE_URL=https://YOUR_PROJECT.baseaf.memfiredb.com
SUPABASE_SERVICE_ROLE_KEY=PLACEHOLDER_MEMFIRE_SERVICE_ROLE_KEY

# 当前图片模型：New API 提供全能模型2/香蕉，Apimart 提供 MJ。
# 不要把这些密钥放进前端配置。
NEWAPI_API_KEY=
# Production uses a direct-origin HTTPS host to avoid proxy timeouts on long image requests.
NEWAPI_API_BASE_URL=https://43-153-201-78.sslip.io
NEWAPI_CATALOG_ADMIN_SECRET=
APIMART_API_KEY=
APIMART_API_BASE_URL=https://api.apimart.ai

# Local R2 is empty; keep media reads pointed at production CDN for previews.
MEDIA_STORAGE_MODE=r2
LOCAL_MEDIA_UPSTREAM=https://api.prompt-hubs.com

ENVIRONMENT=development
PUBLIC_SITE_URL=http://127.0.0.1:5500
CORS_ORIGINS=http://127.0.0.1:5500,http://localhost:5500,https://prompt-hubs.com,https://www.prompt-hubs.com
