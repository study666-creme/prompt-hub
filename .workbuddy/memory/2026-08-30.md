# 2026-08-30

## 卡片库入场动效 + 首屏性能

### 首屏真凶：39 次串行同步 XHR

首屏实际用 **39 次串行同步 XHR** 取内容（6 个 `partials/index-body/part-*.html` +
13 `legacy/script` + 7 `legacy/supabase-sync` + 13 `legacy/features-draft`，约 1.36MB）。
同步 XHR 冻结主线程，串行又让每个分片各付一次 RTT —— 这是「很卡尤其是首屏」的主因。

修法：`index.html` 在 `<head>` 解析期用 `fetch` 一次性并行发出全部请求，文本存进
`window.__PH_PART_STORE__.text`（键 = 分片相对路径，**不含** `?v=`，所以版本号不匹配
也不会取错文件，最坏只是多取一次）。同步加载器先查表，未命中才退回同步 XHR —— 纯增益，
无新失败模式。关键路径 `priority: 'high'`，`features-draft`/`supabase-sync` 用 `'low'`。

拆分加载器是生成物，模板在 `scripts/create-legacy-runtime-split.mjs`，已同步改好；
现有 7 个 root loader 已手工迁移一致。

CSS 也修掉 `@import` 瀑布：`styles.css`/`styles-features.css` 只是清单，20 个真实分片
要多等一次阻塞往返。`index.html` 用 `__PROMPT_HUB_CSS_PRELOAD_START__/END__ <entry>`
标记维护 preload 块（标记带 entry 名，否则多份清单会互相覆盖），版本由
`scripts/create-css-runtime-split.mjs` 重新拆分时同步重写。

实测（本机 HTTP/1.1，56 请求争 6 连接，悲观下限）：同步 XHR 39→6，DCL 960ms→554ms。

### 卡片入场：死代码 + 两个硬约束

`styles/base/part-09.css` 的 `.card-enter-soft` 从未被 JS 使用，且选择器
`#cardsContainer > .card...` 只匹配**直接子**卡片，而列容器瀑布流把卡片放进
`.warehouse-focus-col` —— 永远命中不了。所以卡片一直是硬生生出现。

两个必须遵守的约束（踩过的坑）：
1. **必须用 transition，不能用 animation**。瀑布流分发会 `appendChild` 移动节点，
   移动会重启 CSS animation —— 这正是当年「整片卡片疯狂闪动」导致动效被摘掉的根因。
2. **位移必须放 `.card-media`/`.card-body`，不能放 `.card`**。`.card` 的 `transform`
   已被 hover（`translateY(-6px) scale(1.01x)`）占用，叠在一起会互相打断。

实现：`markWarehouseCardsPending`（布局前隐藏）→ 布局 → `revealWarehouseCards`
（`legacy/script/part-03.js`）。28ms/张错峰、最多 10 档；结束后摘掉 `card-enter-in`
（它覆盖 `.card` 原 transition，留着 hover 会变迟钝）；1500ms 保险丝防卡片停在
`opacity: 0`；`prefers-reduced-motion` 下 JS 直接跳过。

### 新增回归

`scripts/verify-warehouse-card-entrance-browser.mjs`：入场触发/清理/最终不透明/重排不重放，
并用「剥掉预取器」做基线的同步 XHR 与 DCL 对比（不是拍魔法数 —— 绝对值取决于连接并发）。
需要 playwright，本机 chromium 在 `~/AppData/Local/ms-playwright`。

### 注意

- **不要直接跑 `create-css-runtime-split.mjs` 重拆**：`styles.css` 现在只是 `@import`
  清单，脚本会把它当源 CSS 重新切片，等于毁掉真实样式。
- `bump-build.ps1` 对 styles.css 走的是 `$t.Replace($old,$new)`，而 CSS 分片版本是
  `20260718f` 而非 `__APP_BUILD__`，所以那次替换是 no-op —— CSS 分片版本号实际是冻结的。
- `supabase-config.local.js` 的 404 是预期的（仅 localhost 的可选覆盖，代码已 try/catch）。

### 发布与 file:// 预览

- 发布分支 `codex/warehouse-card-entrance-20260830`，提交 `6db3f69`，构建 `20260830c`，
  工作区干净。
- 改完后 `pack-prelude.js` 的文件源横幅依旧在，但 body 加载器的 sync XHR 在 file:// 下
  仍会失败 → 触发 `__PROMPT_HUB_BOOT_RECOVERY__` → "恢复卡藏"页会盖掉横幅，误导用户去
  清缓存。已加 file:// 早返分支：直接显示"请用本地服务器打开"和 `serve-local.ps1` 指引，
  不跑 SW 清理、不重载同 URL。断言在 `verify-warehouse-card-entrance-browser.mjs` 里。
- 本机预览正确流程：在项目文件夹 PowerShell 跑 `.\serve-local.ps1`，开 `http://127.0.0.1:5500`。
  file:// **永远不行**（浏览器禁止跨文件 XHR/fetch，与项目无关）。

### 已上线（Pages 20260830c）

- 发布 SHA `6cd6058`，分支 `codex/warehouse-card-entrance-20260830`，Worker/DB 未动。
- 发布前差点踩大坑：**源码分片专用优化在打包产物里全是死链**。`build-pages-runtime.mjs`
  会把 `legacy/**` 合并成 monolith、body 内联、CSS 内联并剪掉源码片段，于是 head 里的
  预取器（39 次 fetch）+ 20 条 CSS preload 在生产会变成约 59 个 404 —— 比不优化更糟。
  已加 `stripBundleOnlyAssets()` 按标记剥离，标记缺失则构建失败。
- **推论（重要，别再踩）**：生产是打包后的 monolith，没有分片也没有同步 XHR。
  我那套并行预取只改善**本地源码预览**（同步 XHR 39→6、DCL 约 -40%），对线上首屏
  **零收益**。要改善线上首屏必须从打包体积和阻塞脚本入手（如 `pack-imagegen.js`
  385KB 的加载时机）。
- 真正上线生效的是：卡片入场动效（`card-enter-pending` / `card-enter-in` 已在线上
  styles.css 里核到）和 file:// 指引页。
- 验收方式（以后照做）：`APP_ROOT` 指向 `.pages-deploy` 跑浏览器回归，验的是最终产物
  而不是源码树。

### 第二轮：首屏图片（已上线 20260830e，SHA ec04515）

线上实测 1222KB 传输 / FCP ~5.9s，其中图片 590KB 全是未压缩 PNG。

- 三张 hero 图（linche/peishen/scene）生成 WebP q82：468KB → 228KB。`<picture>` +
  PNG 回退。CSS 是后代选择器（`.warehouse-hero-card img` / `.landing-card img`），
  包 `<picture>` 安全。
- logo.png 是 1024×1024（60KB）只显示为 32/30/24px → 新增 `logo-64.png`（3KB）。
  1024 原图保留给 PWA manifest 图标和 og:image。
- **踩坑 1**：hero 图在 `part-02.html`（仓库）和 `part-06.html`（落地页 `/`）**各有一份**，
  两套同时在 DOM 里（未激活的只隐藏）。只改 part-02 → WebP 和 PNG 各下一份，460KB
  一点没省。必须两处一起改。已加断言守住。
- **踩坑 2**：`stage-pages.ps1` 用 `git ls-files` 只拷贝已跟踪文件，新资源未提交会被
  **静默丢弃**、线上变坏图。已加通用护栏：校验 index.html 引用的每个 assets/ 路径都存在。
- **别重编码原始 PNG**：Pillow 无损重编码反而变大 435%（658KB→3520KB），原图已优化过，
  只能换格式。
- 结果：传输 1222KB → **924KB**，图片 590KB → **232KB**，FCP **0.82~0.92s**。

### 测量教训

- 自定义域名 `prompt-hubs.com` 曾出现 **5.2s TLS 握手**（同期 pages.dev 仅 0.6s），
  复测恢复到 ~0.5s，是**瞬时故障**。别把单次慢测量当前端问题；先对比 pages.dev
  和对照站点（google/github）再下结论。
- 资源体积别用 `content-length`（压缩资源常为 0），要用 Resource Timing 的
  `encodedBodySize`。

### 第三轮：非首屏脚本延迟加载（已上线 20260830f，SHA b7604c1）

pack-imagegen / pack-feed / community-public-feed / features-draft / pack-extra
（约 256KB 传输 / 990KB 解码，占脚本 45%）改为首帧后**串行**注入。

- **必须串行、不能并行 async**：features-draft 初始化要用 pack-imagegen 的
  ImageGenJobRunner；feed 包版本校验（不符就 reload 整页）必须跟着 pack-feed 走，
  留在原地会在 pack-feed 到达前误判并 reload。
- **踩坑**：`stage-pages.ps1` 靠扫描 `src="..."` 收白名单，延迟 pack 不再以 src= 出现
  → **被静默裁出暂存包**（源码模式一切正常，只有线上缺文件、功能静默失效）。
  已修：从 `__PH_DEFERRED_PACKS_START__/END__` 标记块读队列。同理改了
  verify-pack-contract（阻塞 src 或延迟队列命中其一即可，但必须命中）。
- 收益真实但很小：本机 A/B（同产物只切换加载方式，5 次中位数）FCP -48ms / DCL -70ms /
  load -48ms。生产这个量级会淹没在网络抖动里。

### 完整成果（本轮首屏）

| | 开始 | 现在 |
|---|---:|---:|
| 传输量 | 1222KB | **917KB**（-25%） |
| 图片 | 590KB | **232KB** |
| FCP - TTFB | 约 595ms | **547ms** |

**大头是图片，不是脚本。**

### 待办 / 未完成

- **分支未推到远端（需用户操作）**：SSH 路走不通（github host key 不在 known_hosts、
  无 gh CLI、无 ssh config）。已把 origin 改成 HTTPS 以配合已安装的
  **Git Credential Manager 2.9.0**，但 GCM 需要 TTY 才能完成 OAuth，我的非交互 shell
  拿不到（`terminal prompts disabled`）。用户在自己终端跑：
  `cd D:\prompt-hub` 然后 `git push -u origin codex/warehouse-card-entrance-20260830`
  （会弹浏览器登录，账号 study666-creme）。
  若想换回 SSH：`git remote set-url origin git@github.com:study666-creme/prompt-hub.git`
  并配好 key。
- `legacy/asset-studio/part-01.js` 的 preset 表仍引用 PNG（含 shenmei.png），属
  asset-studio 独立页面，未动。
- 若继续压首屏，剩下的大头是 **CSS**：10 个 link 共 325KB 传输 / 902KB 解码，其中
  `styles-features.css`（195KB 解码）和 `styles-assets.css`（90KB）是其他页面的，
  首屏根本用不上却阻塞渲染 —— 实测屏蔽它们 boot 正常。按页拆分或改异步加载是下一个
  最大收益点（预估能再省 80KB+ 传输、去掉 2 个阻塞请求）。
- script 侧已延迟 45%，再压收益有限。
- `legacy/asset-studio/part-01.js` 的 preset 表仍引用 PNG（含 shenmei.png），
  属 asset-studio 独立页面，未动。
